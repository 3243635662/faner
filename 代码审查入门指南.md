# Faner 代码审查入门指南（阅读顺序 + 结构解释）

> 目标：帮助审查者按「从地基到顶层、从数据到 UI、从简单到复杂」的顺序快速建立全局认知，避免在 70 个 Dart 文件中迷失。
> 配套：`项目开发报告.md`（架构/配置/规划总览）、`局域网媒体管理器_完整开发文档.md`（需求源）。

---

## 0. 一句话心智模型

> **每台设备既是 HTTP 服务器（发布本机媒体），又是客户端（看别人的媒体）。**
> 所有功能都围绕这个「自包含局域网媒体节点」展开：UI 只是数据的不同视图，Riverpod 是 UI 与数据之间的唯一桥梁。

记住这条主线，看任何文件都能挂上位置。

---

## 1. 推荐阅读顺序（共 7 站，约 90 分钟）

| # | 范围 | 目的 | 预计 |
|---|---|---|---|
| 1 | `main.dart` + `core/`（常量/类型/Result） | 入口与地基概念 | 10 min |
| 2 | `data/models/*` | 数据契约（全项目共用词汇） | 5 min |
| 3 | `data/local/*` + `data/remote/*` | 两个数据源如何抽象 | 15 min |
| 4 | `server/*` | 「本机即 Server」核心实现（含 Range 与安全） | 20 min |
| 5 | `providers/*` | 状态层：把数据接到 UI | 10 min |
| 6 | `features/shared/*` + `features/{local,remote,devices,media_viewer}/*` | 业务页面与复用组件 | 15 min |
| 7 | `features/media_player/*`（**自下而上**） | 最复杂的视频模块，分层精读 | 30 min |
| 8 | `app/router.dart` + `home/home_shell.dart` + `permissions/*` | 导航装配与权限收口 | 10 min |

> 先读 1–5 打地基，再看 6–8。第 7 站务必**自下而上**（models→service→controller→page→widgets），否则会被手势/控件绕晕。

---

## 2. 第 1 站：入口与地基概念

### 2.1 `lib/main.dart`
应用唯一入口。审查要点：
- `MediaKit.ensureInitialized()` **必须在创建 `Player` 之前**调用（播放内核要求）。
- `FlutterForegroundTask.initCommunicationPort()` + `ForegroundService.init()` 在 `runApp` 前完成 —— 通知渠道必须早于「开启共享」就绪（`开发文档 11.4`）。
- `ProviderScope(child: FanerApp())` 是 Riverpod 根。

### 2.2 `lib/core/constants.dart`
全局常量：`basePort=8848`、`maxPortRetries=5`、`serviceType='_lanmedia._tcp'`、`appVersion`、`recentDevicesKey`。所有「魔法数字」都应先来这里找，找不到才考虑是漏抽的常量。

### 2.3 `lib/core/file_type.dart`
`EntryType`（folder/image/video/audio/other）枚举与 `mimeTypeFor` / `fileTypeFromName`。**注意 `audio` 被刻意保留但无播放能力**——这是需求扩展点，审查时别误删。

### 2.4 `lib/core/result.dart`
`sealed class Result<T>`（Ok/Err）。全项目错误处理的统一语言，审查业务逻辑时重点看是否「该返回 Result 的地方却吞了异常」。

> 顺带可扫一眼 `core/tokens.dart`（设计 Token 总入口）、`core/responsive.dart`（最短边判定三条件）、`core/path_utils.dart`（防穿越，第 4 站会用到）。

---

## 3. 第 2 站：数据契约（models）

最底层的「通用词汇」，UI 与 Server 都依赖它：

- `lib/data/models/file_entry.dart`：`FileEntry`（name/path/type/sizeBytes/modifiedAt），含 `fromJson/toJson`、`formattedSize`、`==/hashCode`（按 path+type 判等）。
- `lib/data/models/device_info.dart`：`DeviceInfo`（deviceName/ip/port/isManual），`baseUrl` getter、`==/hashCode`（按 ip+port 判等，用于去重）。

> 审查点：这两个 model 同时被「本地浏览」「远程浏览」「Server JSON 响应」三方复用，改字段会影响全链路，务必谨慎。

---

## 4. 第 3 站：两个数据源

### 4.1 `lib/data/local/local_file_service.dart`（本机）
**上层唯一允许接触 `dart:io` 文件系统的地方**。方法：`sharedRoot()`、`listEntries()`、`createFolder()`、`rename()`、`delete()`、`search()`。
审查要点：
- 所有相对路径都先过 `normalizeRelPath`（防穿越）。
- 排序：文件夹优先 → 文件夹按名、文件按修改时间倒序。
- `search()` 用栈式递归（非递归爆栈），限深 8、限 200 结果，跳过 `.`/Android/`lost+found`。

### 4.2 `lib/data/remote/remote_file_client.dart`（远程）
用 `dio` 封装对「对方 HttpServer」的请求（`list`/`file`/`search`/`info`），返回 `Result`。审查时对照第 6 站的 HTTP API 表看字段是否一致。

### 4.3 `lib/data/remote/discovery_service.dart`（mDNS）
`register()` 广播本机、`discover()` 返回 `Stream<List<DeviceInfo>>`、过滤本机 IP（`_collectMyIps`）。注意：`_resolveIp` 优先取 IPv4；排重用本机网卡地址集合。

---

## 5. 第 4 站：本机 HTTP 服务器（核心）

这是「每台设备同时是 Server」的关键实现，也是技术含量最高的部分。

### 5.1 `lib/server/media_http_server.dart`
`HttpServer.bind(InternetAddress.anyIPv4, basePort+i)`，`for` 循环 +1 重试最多 5 次；`server.listen(router.handle)`。

### 5.2 `lib/server/router.dart`
按 `request.uri.path` 的 `switch` 分发到 5 个 handler，全局 `try/catch` 兜底 `500`。

### 5.3 `lib/server/server_context.dart` ⭐安全重点
- `PathGuard.resolve()`：相对路径 → `root` 内绝对路径，越界返回 `null`。**所有 handler 第一件事就是过 PathGuard**。
- `writeJson` / `respond` / `forbidden` / `notFound` 等响应工具函数。

### 5.4 `lib/server/handlers/*` ⭐审查重点
- `info_handler.dart`：返回设备信息。
- `list_handler.dart`：列目录，路径不存在 `404`，穿越 `403`。
- **`file_handler.dart`（全项目最难）**：Range 解析、`206`/`416`、流式 `openRead(start, end+1)`、`Accept-Ranges`。**禁止 `readAsBytes()`**。
- `search_handler.dart`：委托 `LocalFileService.search`。
- `thumbnail_handler.dart`：视频缩略图。

> 安全审查清单：用 `?path=../../../etc` 请求 list/file，必须 `403`；Range 必须 `206` 且 `Content-Range` 正确。见 `项目开发报告.md` 第 8 节。

### 5.5 `lib/server/foreground_service.dart`
前台服务保活封装（不含业务逻辑，仅保活 + WiFi 锁）。注意 `init()` 必须早调用，`startCallback` 是 `@pragma('vm:entry-point')` 的独立 isolate 入口。

---

## 6. 第 5 站：状态层（providers）

UI 与数据之间的桥。建议对照 `项目开发报告.md` 第 6 节表格看：

- `services_provider.dart`：三个服务单例。
- `server_provider.dart`：`ServerController`，`start()` 串联 HttpServer + 前台服务 + mDNS 注册；失败各步独立 `try/catch` 不影响主流程。
- `discovery_provider.dart`：`discoveryProvider`（Stream）+ `manualDevicesProvider`（持久化）+ `deviceListProvider`（合并去重）。
- `browser_provider.dart`：`local/remote` 浏览 `FutureProvider.family`、路径 `Notifier`、双栏选中项 `selectedEntryProvider`。
- `settings_provider.dart`：设置项 + `shared_preferences` 持久化，`ensureLoaded()` 异步加载闸门。

> 审查点：`localPathProvider` / `remotePathProvider` 是全局路径，配合 `router.dart` 的 `onExit` 实现「系统返回键逐层回退目录」（`PopScope` 与 go_router 有兼容坑，已规避）。

---

## 7. 第 6 站：业务页面与复用组件

### 7.1 `lib/features/shared/`（复用层，先看）
- `browser_scaffold.dart`：响应式主从双栏容器，`LayoutBuilder` + `Responsive.useSplitLayout`（平板+横屏+宽≥840 才双栏）。
- `file_grid_view.dart` / `file_list_view.dart`：本地/远程共用的两种视图。
- `file_thumbnail.dart`：缩略图（本地缩略图 / 远程 `cached_network_image`）。
- `detail_panel.dart`：双栏右侧详情。
- `empty_state.dart` / `error_view.dart` / `skeleton.dart`：状态视图。

### 7.2 `lib/features/local_browser/local_browser_page.dart`
本地浏览：消费 `localBrowserProvider` + `localPathProvider`，点击文件 → 图片推 `/image`、视频推 `/video` 顶级路由（extra 为命名 record）。

### 7.3 `lib/features/remote_browser/remote_browser_page.dart`
远程浏览：复用 `shared` 组件，数据源换成 `remoteBrowserProvider`，device 来自路由 `extra`。

### 7.4 `lib/features/devices/devices_page.dart`
设备列表：消费 `deviceListProvider`，手动 `IP:端口` 输入兜底（先 `/api/info` 校验连通性才加入 `manualDevicesProvider`）。

### 7.5 `lib/features/media_viewer/*`（图片）
`image_view.dart`（photo_view 缩放）+ `image_viewer_page.dart`（顶部路由，支持多图滑动）+ `media_source.dart`（`MediaSource` 统一描述图片/视频来源，路由 extra 的载体）。

---

## 8. 第 7 站：视频播放模块（最复杂，务必自下而上读）

```
models → service → controller → page → widgets
```

### 8.1 `features/media_player/models/`
- `media_item.dart`：`MediaItem`（uri/isLocal/id 等），列表与断点续播键的来源。
- `player_state.dart`：`PlayerState` + `PlayerStatus` 状态机（idle/loading/playing/paused/buffering/completed/error）。

### 8.2 `features/media_player/services/media_player_service.dart` ⭐
**整个项目唯一直接依赖 `media_kit` 的文件**。只暴露语义化数值（position/duration/volume/rate）与控制方法（`open`/`play`/`pause`/`seek`/`setRate`/`setVolume`）。`open` 先 `play:false` 再 `seek` 再 `play`，避免首帧闪跳。

### 8.3 `features/media_player/controllers/media_player_controller.dart` ⭐
播放会话「大脑」：
- 状态全部来自 `MediaPlayerService` 的**状态流订阅**（无 Timer 轮询）。
- 业务逻辑：连播（顺序/随机/循环 `VideoPlayMode`）、断点续播（`VideoResumeStore`）、生命周期（`didChangeAppLifecycleState` 切后台暂停）、屏幕常亮（`WakelockPlus`，内嵌预览不霸屏）。
- `position` 用独立 `ValueNotifier` 高频更新，避免 `notifyListeners` 拖累整个控件层。
- 错误经 `describePlayerError` 中文化分类。

### 8.4 `features/media_player/pages/video_player_page.dart`
顶级全屏路由页：创建 `MediaPlayerController`，装配 `MediaPlayerView` + 控件层 + 播放列表面板；「显示模式」切换（锁横屏+沉浸）而非新开路由。

### 8.5 `features/media_player/widgets/`（9 个）
- `video_surface.dart`：把 `VideoController` 交给 `Video` 渲染。
- `media_player_view.dart`：组合视频面 + 手势层 + 控件层。
- `player_controls_overlay.dart` / `player_progress_bar.dart` / `player_playlist_panel.dart`：控件。
- `player_indicators.dart` / `player_status_views.dart`：缓冲/错误/完成状态视图。
- `video_preview_panel.dart`：双栏右侧内嵌预览（embedded 模式，不接管系统 UI）。
- `playback_speed_sheet.dart`：倍速面板。

> 审查技巧：手势冲突是该模块最大坑。`onHorizontalDragDown` 记录基准 + `_seekActivateThreshold=18` 位移阈值后才进入快进模式，防止与亮度/音量竖向拖动打架。

---

## 9. 第 8 站：导航装配与权限收口

### 9.1 `lib/app/router.dart`
`go_router` 路由表：
- `StatefulShellRoute.indexedStack` 承载 3 个 tab（文件/设备/设置），切 tab 保活。
- 顶级路由 `/remote`、`/image`、`/video`（extra 传 `MediaSource` 列表 + index + fromSplit）。
- `onExit` 拦截系统返回键：路径非空时回上级目录而非退出。

### 9.2 `lib/home/home_shell.dart`
导航壳：`LayoutBuilder` 按宽度 ≥600 在 `NavigationRail`（平板侧栏）与 `NavigationBar`（手机底栏）间切换；`initState` 里 `_bootstrap` 加载设置并按需启动 Server。

### 9.3 `lib/permissions/storage_permission.dart`
存储权限封装：优先 Android 13+ 细分媒体权限，`MANAGE_EXTERNAL_STORAGE` 引导跳系统设置页授权。

---

## 10. 审查时的高频踩坑点速查

| 关注点 | 位置 | 正确行为 |
|---|---|---|
| 路径穿越 | `core/path_utils.dart` + `server/server_context.dart` | 含 `..` 必 `403` |
| 大文件内存 | `server/handlers/file_handler.dart` | 仅 `openRead` 流式，无 `readAsBytes` |
| 播放状态来源 | `media_player/controllers` | 订阅内核流，无 Timer 轮询 |
| 内核封装边界 | `media_player/services/media_player_service.dart` | 唯一碰 `media_kit` 处 |
| 平板误判 | `core/responsive.dart` | 用最短边，不判 isTablet 即双栏 |
| 全屏重建缓冲 | `video_player_page.dart` | 显示模式切换，非独立路由 |
| release 闪退 | `android/app/proguard-rules.pro` | 保留 WorkManager/Room 类 |
| 明文拦截 | `AndroidManifest.xml` | `usesCleartextTraffic=true` |
| 返回键行为 | `app/router.dart` onExit | 逐层回退目录 |

---

## 11. 建议的「第一刀」审查路径（最小可行理解）

如果时间有限，按此 9 个文件即可掌握全局：

1. `main.dart`
2. `core/constants.dart`
3. `data/models/file_entry.dart`
4. `server/handlers/file_handler.dart`
5. `server/server_context.dart`
6. `providers/server_provider.dart`
7. `features/shared/browser_scaffold.dart`
8. `features/media_player/services/media_player_service.dart`
9. `app/router.dart`

读完这 9 个，再横向展开其余文件会非常顺。
