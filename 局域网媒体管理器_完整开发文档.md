# 局域网文件管理 + 多媒体互看 App —— 完整开发文档

> 本文档面向执行开发的 Agent（如 Claude Code）。请严格按文档顺序与分阶段要求实施，不要提前引入后续阶段的依赖或功能。每完成一个里程碑，先自测通过再进入下一个。

---

## 0. 项目目标与约束

- **形态**：Flutter App，仅适配 **Android 手机 + 平板**，不考虑 iOS / Web / 桌面。
- **核心功能**：
  1. 本地文件管理（浏览、查看本机文件，重点是图片/视频）。
  2. 局域网内多台同款 App 互相发现，互相浏览、播放对方设备上的媒体文件（类似 FTP/DLNA 互看，用 HTTP 实现，非公网）。
  3. 每台设备**同时是 Server（对外发布本机媒体）和 Client（浏览别的设备）**。
- **非目标**（明确不做，避免过度设计）：
  - 不做用户账号系统、不做云端同步、不做多平台适配、不做公网穿透。
  - 不追求 Google Play 上架合规，可直接使用高权限（`MANAGE_EXTERNAL_STORAGE` 等）。
- **使用场景**：仅自己和家人在同一局域网使用，安全模型按"内网可信"设计，但仍需路径穿越等基本防护。

---

## 1. 项目初始化命令（已初始化）

```bash
flutter create --org com.faner.app --platforms=android faner
cd faner
```

`--platforms=android` 只生成安卓工程目录，避免无用的 iOS/Web/桌面文件。

创建后立即修改 `android/app/build.gradle`（或 `.kts`）：

```gradle
defaultConfig {
    minSdkVersion 26        // Android 8.0，个人项目不必兼容更老设备，减少存储权限兼容代码
    targetSdkVersion flutter.targetSdkVersion
}
```

---

## 2. 技术栈与安装命令（严格分阶段，禁止一次性全装）

### 阶段 A（MVP 必需，项目启动时安装）

```bash
flutter pub add flutter_riverpod
flutter pub add dio
flutter pub add nsd
flutter pub add video_player
flutter pub add path_provider
flutter pub add permission_handler
flutter pub add shared_preferences
```

| 用途 | 包 |
|---|---|
| 状态管理 | flutter_riverpod |
| HTTP 客户端 | dio |
| 局域网服务发现 | nsd |
| 视频播放 | video_player |
| 路径获取 | path_provider |
| 权限申请 | permission_handler |
| 简单键值存储 | shared_preferences |
| HTTP 服务端 | `dart:io` 的 `HttpServer`（SDK 自带，无需安装） |
| 主题 | Material 3 原生 |
| 路由 | Navigator 1.0（自封装 `AppNavigator`，不引入 go_router） |

### 阶段 B（MVP 跑通闭环后再执行）

```bash
flutter pub add cached_network_image
flutter pub add photo_view
flutter pub add flutter_foreground_task
flutter pub add flex_color_scheme
flutter pub add flutter_animate
flutter pub add lucide_icons
```

`chewie` 是否需要视 `video_player` 原生控件够不够用而定，可选安装：`flutter pub add chewie`。

> 图标库 pub.dev 上同时存在 `lucide_icons`、`lucide_icons_flutter`、`flutter_lucide` 等多个同类包，安装前请先上 pub.dev 核实当前维护状态和 API，选一个即可，不要重复安装。

### 阶段 C（功能打磨期，按需安装）

```bash
flutter pub add hive
flutter pub add hive_flutter
flutter pub add video_thumbnail
flutter pub add flutter_image_compress
```

**执行规则**：初始化项目时 `pubspec.yaml` 只包含阶段 A 的依赖。阶段 B、C 的包在对应里程碑开发时才安装，不要提前引入。

---

## 3. 整体架构

```
┌──────────────────────────────────────────────────────────┐
│                        Flutter App                         │
│                                                              │
│   ┌───────────┐   ┌────────────┐   ┌────────────────────┐ │
│   │    UI      │   │  Riverpod  │   │     Repository      │ │
│   │ (Widgets)  │◄──┤ (Providers)│◄──┤ (业务逻辑/数据聚合)   │ │
│   └───────────┘   └────────────┘   └──────────┬──────────┘ │
│                                                 │            │
│                    ┌────────────────────────────┼─────────┐ │
│                    │                             │         │ │
│            ┌───────▼──────┐            ┌────────▼───────┐ │ │
│            │LocalFileService│         │RemoteFileClient │ │ │
│            │ (本机文件系统)  │          │  (Dio 请求封装) │ │ │
│            └───────────────┘           └────────┬────────┘ │ │
│                                                   │          │ │
│            ┌──────────────────┐         (HTTP over LAN)     │ │
│            │  MediaHttpServer  │◄─────────────────┘         │ │
│            │ (dart:io Server)  │                             │ │
│            └────────┬──────────┘                             │ │
│                     │                                        │ │
│            ┌────────▼──────────┐                             │ │
│            │ DiscoveryService   │ (nsd: 广播自己+扫描邻居)     │ │
│            └────────────────────┘                             │ │
└──────────────────────────────────────────────────────────┘
```

**数据流向**：
- 本机浏览：`UI → Provider → LocalFileService → dart:io 文件系统`
- 浏览远程设备：`UI → Provider → RemoteFileClient(Dio) → 对方设备的 MediaHttpServer`
- 被别人浏览：本机 `MediaHttpServer` 接收请求 → 读取本机文件系统 → 返回 JSON/文件流
- 设备互相发现：`DiscoveryService` 双向广播+扫描 → 拿到 `ip:port` → 交给 `RemoteFileClient` 使用

---

## 4. 目录结构

```
lib/
├── main.dart
├── app/
│   ├── app.dart                     # MaterialApp 根组件，主题装配
│   └── navigation/
│       └── app_navigator.dart       # 统一封装 Navigator.push 调用
│
├── core/
│   ├── constants.dart               # 端口号、mDNS 服务类型字符串等常量
│   ├── file_type.dart               # 文件类型枚举、后缀→MIME 判断
│   ├── breakpoints.dart             # 响应式布局断点（手机/平板）
│   └── result.dart                  # 统一的 Result<T> 封装
│
├── data/
│   ├── models/
│   │   ├── file_entry.dart
│   │   └── device_info.dart
│   ├── local/
│   │   └── local_file_service.dart  # 本机文件系统读取封装
│   └── remote/
│       ├── remote_file_client.dart  # Dio 封装
│       └── discovery_service.dart   # nsd 封装
│
├── server/
│   ├── media_http_server.dart
│   ├── router.dart
│   └── handlers/
│       ├── info_handler.dart
│       ├── list_handler.dart
│       └── file_handler.dart        # 重点：Range 处理
│
├── features/
│   ├── local_browser/
│   ├── devices/
│   ├── remote_browser/
│   ├── media_viewer/
│   │   ├── image_viewer_page.dart
│   │   └── video_player_page.dart
│   ├── shared/
│   │   ├── file_grid_view.dart      # 手机/平板通用的文件网格组件
│   │   └── browser_scaffold.dart    # 响应式主从布局容器（见第9节）
│   └── settings/
│
└── permissions/
    └── storage_permission.dart
```

---

## 5. 核心数据模型

### 5.1 FileEntry

```dart
enum EntryType { folder, image, video, other }

class FileEntry {
  final String name;
  final String path;          // 相对于共享根目录的相对路径
  final EntryType type;
  final int sizeBytes;        // 文件夹可为 -1（未计算）
  final DateTime modifiedAt;

  FileEntry({
    required this.name,
    required this.path,
    required this.type,
    required this.sizeBytes,
    required this.modifiedAt,
  });

  factory FileEntry.fromJson(Map<String, dynamic> json) => FileEntry(
        name: json['name'],
        path: json['path'],
        type: EntryType.values.byName(json['type']),
        sizeBytes: json['sizeBytes'],
        modifiedAt: DateTime.parse(json['modifiedAt']),
      );

  Map<String, dynamic> toJson() => {
        'name': name,
        'path': path,
        'type': type.name,
        'sizeBytes': sizeBytes,
        'modifiedAt': modifiedAt.toIso8601String(),
      };
}
```

### 5.2 DeviceInfo

```dart
class DeviceInfo {
  final String deviceName;
  final String ip;
  final int port;
  final bool isManual;

  DeviceInfo({
    required this.deviceName,
    required this.ip,
    required this.port,
    this.isManual = false,
  });

  String get baseUrl => 'http://$ip:$port';
}
```

---

## 6. 网络协议设计（HTTP API）

固定端口：`8848`（写入 `core/constants.dart`，被占用则自动 +1 重试，最多 5 次）。

mDNS 服务类型：`_lanmedia._tcp`（**不要**用通用的 `_http._tcp`，否则会扫到局域网内无关的 HTTP 服务）。

### 6.1 `GET /api/info`

```json
{
  "deviceName": "Lebron-Phone",
  "appVersion": "1.0.0",
  "sharedRootLabel": "内部存储"
}
```

### 6.2 `GET /api/list?path=xxx`

`path` 为相对路径，空字符串代表共享根目录。

```json
{
  "currentPath": "DCIM/Camera",
  "entries": [
    {
      "name": "photo1.jpg",
      "path": "DCIM/Camera/photo1.jpg",
      "type": "image",
      "sizeBytes": 2345123,
      "modifiedAt": "2026-08-01T10:20:00.000Z"
    }
  ]
}
```

路径不存在返回 `404`；**路径穿越（包含 `..`）一律返回 `403`，这是安全底线，list_handler 和 file_handler 都必须做规范化校验**。

### 6.3 `GET /api/file?path=xxx` —— 全项目技术含量最高的部分

**必须支持 Range 请求**，逻辑如下：

1. 解析 `Range: bytes=start-end` 请求头（`end` 可能缺省）。
2. 无 Range 头：返回完整文件，状态码 `200`，带正确 `Content-Type` 和 `Content-Length`。
3. 有 Range 头：
   - 校验范围合法性，非法返回 `416`。
   - 返回 `206 Partial Content`，响应头包含：
     ```
     Content-Range: bytes {start}-{end}/{totalSize}
     Content-Length: {end-start+1}
     Accept-Ranges: bytes
     ```
   - 用 `RandomAccessFile` 从 `start` 开始流式读取到 `end`，**禁止一次性读入内存**（视频文件可能几个 GB，用 `readAsBytes()` 会导致内存爆掉、App 闪退）。
4. 所有响应都带 `Accept-Ranges: bytes`。

**Content-Type 判断表**：

| 后缀 | Content-Type |
|---|---|
| jpg/jpeg | image/jpeg |
| png | image/png |
| gif | image/gif |
| webp | image/webp |
| mp4 | video/mp4 |
| mkv | video/x-matroska |
| mov | video/quicktime |
| 其他 | application/octet-stream |

---

## 7. mDNS 服务发现设计

`data/remote/discovery_service.dart` 暴露：

```dart
Future<void> registerService({required String deviceName, required int port});
Stream<DeviceInfo> discoverDevices();
void stopDiscovery();
```

- 注册：App 启动、Server 启动成功后调用，`type` 固定 `_lanmedia._tcp`。
- 扫描：进入设备列表页时启动，退出时停止，注意过滤掉本机自己广播的服务（用设备名或本机 IP 排重）。
- **手动连接兜底（必须有）**：设备列表页提供手动输入 `IP:端口` 入口，先请求 `GET /api/info` 校验连通性成功才加入列表，存入 `shared_preferences`（阶段A）或 `hive`（阶段C）做"最近连接"。

---

## 8. 模块详细设计

### 8.1 Server 模块
- `MediaHttpServer` 提供 `start()`/`stop()`/`isRunning`，绑定全局 Provider（`serverControllerProvider`），App 启动时自动 `start()`。
- `router.dart` 用简单 if-else 或 `Map<String, Handler>` 按路径前缀分发，不需要正则路由引擎。
- 共享根目录：阶段A固定为整个外部存储根目录；阶段C可做成"自选共享文件夹"设置项。

### 8.2 Local File 模块
- `LocalFileService` 用 `Directory.list()` 遍历，按类型分类。
- 排序：文件夹优先，其次修改时间倒序（阶段C可做成可切换项）。
- **上层 UI 只能调用 `LocalFileService` 暴露的抽象方法（`listEntries(path)`、`getFile(path)`），不能直接调 `dart:io`**，以便未来切换 SAF/MediaStore 时只改这一层。

### 8.3 Remote Browser 模块
- 复用 `FileEntry` 模型，与 `local_browser` 共用同一套 `FileGridView`/`FileListView` 组件（放在 `features/shared/`），通过回调区分数据源，避免重复造轮子。

### 8.4 Media Viewer 模块
- 图片：阶段A用 `Image.network(url)`；阶段B换 `cached_network_image` + `photo_view` 支持缩放。
- 视频：`VideoPlayerController.networkUrl` 直接传远程 URL，因为 Server 已支持 Range，播放器原生拖动/缓冲逻辑可直接工作。

### 8.5 权限模块
- `storage_permission.dart` 封装权限检查与申请流程（见第11节）。

### 8.6 Settings 模块
- 阶段A：设备名称设置（默认取机型名）、Server 开关。
- 阶段C：共享目录选择、最近连接历史、收藏夹。

---

## 9. 平板适配设计（手机+平板统一代码，响应式而非分设备类型）

### 9.1 布局断点
```dart
// core/breakpoints.dart
class Breakpoints {
  static const compact = 600;  // < 600dp：手机
  static const medium = 840;   // 600~840dp：竖屏大屏/小平板
  // >= 840dp：横屏平板/大屏
}
```
用 `MediaQuery.sizeOf(context).width` 判断，**不要判断"是不是平板设备"，只判断当前可用宽度**，因为平板转竖屏后布局需求和手机一致。

### 9.2 主从（Master-Detail）双栏布局 —— 平板核心适配点
宽度 ≥ 840dp 时，左侧固定文件列表面板，右侧原地预览图片/视频（不走 `Navigator.push`），两个面板共享 Riverpod 的"当前选中项"状态：

```dart
// features/shared/browser_scaffold.dart
class BrowserScaffold extends ConsumerWidget {
  @override
  Widget build(context, ref) {
    final isWide = MediaQuery.sizeOf(context).width >= Breakpoints.medium;
    if (isWide) {
      return Row(
        children: [
          Expanded(flex: 2, child: FileListPanel()),
          const VerticalDivider(width: 1),
          Expanded(flex: 3, child: DetailPanel()),
        ],
      );
    }
    return FileListPanel(); // 手机：保持原来的 push 到详情页
  }
}
```

### 9.3 导航容器随宽度切换
```dart
final isWide = MediaQuery.sizeOf(context).width >= Breakpoints.compact;
isWide
  ? NavigationRail(destinations: [...])   // 平板/横屏：侧边栏
  : NavigationBar(destinations: [...]);   // 手机：底部栏
```

### 9.4 网格列数自适应
```dart
final width = MediaQuery.sizeOf(context).width;
final crossAxisCount = (width / 140).floor().clamp(2, 8); // 每格约140dp
```
禁止写死 `crossAxisCount`，否则平板要么缩略图巨大要么大片留白。

### 9.5 其他平板注意点
- **不锁横竖屏**，两种方向都要测试，尤其视频全屏页的横屏适配。
- **分屏/多窗口**：安卓平板常用分屏多任务，状态必须放在 Riverpod 而不是 Widget 局部 `State`，避免窗口尺寸变化重建后数据丢失；开发时实测把 App 拖到 1/3 宽小窗验证不会挤爆。
- **触摸目标不要等比放大**：屏幕大不代表手指大，按钮可点击区域始终 ≥ 48dp，平板上靠"一屏展示更多内容"而不是"控件放大"来利用空间。
- **测试环境**：至少准备一个手机 AVD + 一个平板 AVD（如 Pixel Tablet），两种分辨率、两种方向都跑一遍完整流程。

---

## 10. Riverpod Provider 清单

| Provider | 类型 | 职责 |
|---|---|---|
| `serverControllerProvider` | StateNotifierProvider | 控制 MediaHttpServer 启停 |
| `discoveryProvider` | StreamProvider | 持续输出扫描到的 DeviceInfo 列表 |
| `localBrowserProvider` | FutureProvider.family<List<FileEntry>, String> | 按路径加载本地文件 |
| `remoteBrowserProvider` | FutureProvider.family<List<FileEntry>, (DeviceInfo, String)> | 按设备+路径加载远程文件 |
| `deviceListProvider` | StateNotifierProvider | 合并 mDNS 发现结果+手动设备+去重 |
| `selectedEntryProvider` | StateProvider<FileEntry?> | 平板主从布局中"当前选中项"，供左右面板共享 |
| `settingsProvider` | StateNotifierProvider | 设备名、Server 开关等 |

---

## 11. Android 配置与权限（关键，容易踩坑）

### 11.1 AndroidManifest.xml 必需项

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
<uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
<!-- 阶段B加入前台服务后再补充 -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
```

`<application>` 标签必须加（**极其关键，否则所有跨设备 HTTP 请求会被系统静默拦截**）：

```xml
<application
    android:usesCleartextTraffic="true"
    ...>
```

> **原因**：Android 9（API 28）起系统默认禁止明文 HTTP 流量，而本项目全程是局域网 HTTP 通信。不加这一条，请求会直接失败或超时，且报错不直观，容易被误判成 mDNS 或业务逻辑问题。因为本 App 永远不出局域网，全局允许明文流量是合理的，不需要额外写 `network_security_config.xml`。

### 11.2 权限申请注意事项

- `MANAGE_EXTERNAL_STORAGE` 是特殊权限，不能像普通权限一样弹窗直接给，必须引导用户跳转系统设置页单独授权（`permission_handler` 的 `Permission.manageExternalStorage.request()`）。
- Android 13（API 33）起系统拆分出了 `READ_MEDIA_IMAGES`、`READ_MEDIA_VIDEO` 等细分权限，部分厂商 ROM 权限弹窗行为不完全一致，建议一并检查这两个权限，不要只依赖 `manageExternalStorage`。

### 11.3 mDNS 已知问题
`nsd` 包底层走系统 `NsdManager`，一般不需要手动处理组播锁，但个别国产定制 ROM 会限制后台组播导致扫描不到设备。如果出现"手动连接正常、自动发现不了"的情况，先怀疑这里，别怀疑代码逻辑写错。

### 11.4 前台服务注意事项
`flutter_foreground_task` 在 Android 8+ 需要提前创建好通知渠道（NotificationChannel），必须在 Server 启动逻辑最前面初始化，不要等用户点"开启共享"才临时创建，否则第一次点击可能没反应。

---

## 12. 开发阶段与里程碑（严格按此顺序推进）

### 里程碑 1：本地文件浏览器
- 初始化项目，接入阶段A依赖。
- 实现存储权限申请流程。
- 实现 `LocalFileService` + 本地浏览页，能区分图片/视频/文件夹并展示缩略图。
- **验收**：真机上 DCIM 等目录的图片正常显示。

### 里程碑 2：最小可用 Server + 手动互联
- 实现 `MediaHttpServer`（先不做 Range，只返回完整文件）+ 三个 handler。
- 两台设备手动输入 IP:端口互联，能拉到对方文件列表 JSON。
- **验收**：设备A能看到设备B的目录结构（暂不要求能播放视频）。

### 里程碑 3：Range 支持 + 视频播放打通
- 完整实现第6.3节的 Range 逻辑。
- 接入 `video_player`，验证远程 URL 拖动进度条正常工作。
- **验收**：播放 500MB+ 视频，拖动进度条无需重新缓冲整个文件，跳转响应在1-2秒内。

### 里程碑 4：mDNS 自动发现
- 接入 `nsd`，实现注册+扫描，替代纯手动输入，保留手动输入兜底。
- **验收**：两台设备打开App后几秒内自动互相看到，无需手动操作。

### 里程碑 5：平板响应式布局
- 实现第9节的主从布局、导航容器切换、网格列数自适应。
- **验收**：同一套代码在手机和平板（含横竖屏切换、分屏小窗）下均正常展示，无布局溢出或裁切。

### 里程碑 6：后台保活
- 接入 `flutter_foreground_task`，Server 运行时显示常驻通知。
- **验收**：设备A锁屏5分钟后，设备B仍能正常拉取文件/播放视频。

### 里程碑 7：体验打磨（按需选做）
- 图片缓存/手势缩放、缩略图生成缓存、主题美化、收藏夹、最近连接历史。

---

## 13. 验收标准与测试要点

- **安全底线（必测）**：用 `path=../../../etc` 请求 `/api/list`、`/api/file`，必须返回 `403`。
- **Range正确性（必测）**：`curl -r 1000-2000 http://ip:port/api/file?path=xxx`，验证返回 `206` 且 `Content-Range` 正确。
- **明文流量（必测）**：确认 `usesCleartextTraffic="true"` 已配置，真机上两台设备间请求不被系统拦截。
- **大文件性能**：4K视频（1GB+）局域网播放，记录首帧加载时间、拖动响应时间。
- **弱网/断连处理**：关闭Wi-Fi或对方App退出后，客户端要有超时和友好提示，不能一直转圈或崩溃。
- **端口冲突**：模拟8848端口被占用，验证Server自动切换端口并重新广播。
- **平板专项**：横竖屏切换、分屏小窗、不同宽度断点下网格列数变化，均需真机/平板模拟器实测。

---

## 14. 已知风险与应对汇总

| 风险 | 应对方案 |
|---|---|
| HTTP明文流量被系统拦截 | manifest加 `usesCleartextTraffic="true"`（第11.1节） |
| mDNS在AP隔离/访客网络下失效 | 保留手动IP连接入口 |
| 厂商ROM后台杀进程激进 | 前台服务+引导关闭电池优化 |
| Android 11+分区存储限制 | 直接申请 `MANAGE_EXTERNAL_STORAGE`，并做FileSystem抽象层 |
| Android 13+细分媒体权限 | 一并检查 `READ_MEDIA_IMAGES`/`READ_MEDIA_VIDEO` |
| 视频文件过大导致内存爆掉 | file_handler必须流式读取，禁止 `readAsBytes()` |
| 局域网内多App互相干扰 | 自定义mDNS服务类型 `_lanmedia._tcp` |
| 平板分屏/多窗口状态丢失 | 状态放Riverpod而非Widget局部State |
| 部分ROM组播受限导致mDNS扫描不到设备 | 优先排查此项，而非怀疑业务代码 |

---

## 附：给 Agent 的执行提示

1. 严格按"里程碑1→7"顺序开发，每个里程碑做完先自测通过再进入下一个，不要跳跃或并行开发多个里程碑。
2. 初始化项目的 `pubspec.yaml` 只包含第2节阶段A的依赖，阶段B/C的包等到对应里程碑再添加，并在提交说明中注明原因。
3. `file_handler.dart` 的 Range 实现和 `usesCleartextTraffic` 配置是两个最容易被忽略但一旦漏掉会导致核心功能完全不可用的点，务必对照第6.3节和第11.1节逐项核对。
4. 平板适配走"响应式断点"思路，不要写"if isTablet"这种设备类型判断分支，统一用宽度断点（第9节）。
5. 所有跨设备通信仅通过第6节定义的HTTP API进行，不引入mDNS广播之外的其他自定义协议。
6. UI组件尽量在 `local_browser` 与 `remote_browser` 之间复用，放在 `features/shared/` 下，避免重复造轮子。
7. 项目根目录建议维护一份 `README.md`，记录当前完成到哪个里程碑，方便后续会话或协作者快速接续上下文。
