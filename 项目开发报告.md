# Faner 项目开发报告

> 局域网文件管理 + 多媒体互看 App（Flutter / Android 手机 + 平板，自用）
> 版本：`2.1.1+4` ｜ 编写日期：2026-09-16
> 配套文档：`局域网媒体管理器_完整开发文档.md`（需求与技术方案事实源）、`README.md`（实现现状）、`markdown/`（专项优化方案）

---

## 1. 项目概述

Faner 是一款**只在局域网内使用**的 Android 应用，每台设备同时扮演两种角色：

- **Server（服务端）**：把本机存储通过 `HttpServer` 对外发布，支持 `Range` 流式读取；
- **Client（客户端）**：浏览并播放局域网内其它同款设备上的图片/视频。

核心场景是「自己和家人在同一 Wi-Fi 下互看对方手机/平板的媒体」，安全模型按「内网可信」设计，但保留路径穿越等基本防护。不涉及账号、云端、公网穿透。

### 1.1 已完成功能

| 模块 | 状态 | 说明 |
|---|---|---|
| 本地文件浏览与管理 | ✅ | 图片/视频/音频/其他分类、缩略图、筛选、递归搜索、新建/重命名/删除 |
| 最小 Server + 手动互联 | ✅ | `/api/info`、`/api/list`、`/api/file`、`/api/search`、`/api/thumbnail` |
| Range 流式 + 视频播放 | ✅ | `media_kit`（libmpv）内核，拖动秒级响应 |
| mDNS 自动发现 | ✅ | `_lanmedia._tcp` 广播+扫描，手动 `IP:端口` 兜底 |
| 平板响应式布局 | ✅ | 主从双栏、导航栏/侧边栏切换、网格列数自适应、横屏适配 |
| 后台保活 | ✅ | `flutter_foreground_task` 前台服务 + 常驻通知 + WiFi 锁 |
| 体验打磨 | 🔶 | 图片缓存/手势缩放、断点续播、连播模式、灵敏度调节；仍有部分未做项 |

### 1.2 与原始需求文档的关键差异（已实现为合理扩展）

- **保留 `EntryType.audio`**：枚举、后缀/MIME、图标与「音频」筛选 chip 均保留 —— 音频文件可浏览、筛选、HTTP 下载，但点击只提示「暂不支持播放音频文件」（未引入音频播放器）。
- **播放内核由 `video_player` 迁移为 `media_kit`**：获得 MKV / 多音轨 / 字幕等格式覆盖与更稳定的解码（详见 `markdown/视频播放优化方案.md`）。
- **全屏采用「显示模式」而非独立路由**：播放页本身即沉浸式全屏路由，页内切换显示模式，避免重建播放器造成二次缓冲。
- **补充本地增删改查与搜索**：`/api/search` 为需求文档之合理补充。
- **引入 `device_info_plus`**：默认设备名取机型名。

---

## 2. 技术栈与依赖

| 用途 | 包 | 版本 |
|---|---|---|
| 状态管理 | `flutter_riverpod` | ^3.4.3（Notifier / FutureProvider / StreamProvider） |
| 路由 | `go_router` | ^18.0.1（Navigator 2.0） |
| HTTP 客户端 | `dio` | ^5.11.1 |
| 服务发现 | `nsd` | ^5.0.1（mDNS） |
| 播放内核 | `media_kit` / `media_kit_video` / `media_kit_libs_video` | 1.2.6 / 2.0.1 / 1.0.7（libmpv） |
| 后台保活 | `flutter_foreground_task` | ^11.0.3 |
| 远程图片缓存 | `cached_network_image` | ^4.0.0 |
| 图片缩放 | `photo_view` | ^0.15.0 |
| 权限 | `permission_handler` / `device_info_plus` | 13.0.2 / 13.2.0 |
| 键值存储 | `shared_preferences` | ^2.5.5 |
| 路径 | `path_provider` | ^2.1.6 |
| 视频缩略图 | `fc_native_video_thumbnail` | ^3.0.1 |
| 其它 | `wakelock_plus`、`share_plus`、`flutter_animate`、`flutter_lucide`、`shimmer` | — |
| 服务端 | `dart:io` 的 `HttpServer`（SDK 自带） | — |
| 主题 | Material 3 原生 + 设计 Token（`lib/core/tokens.dart`） | — |

> 注意：`pubspec.yaml` 的 `description` 仍是模板默认文案「A new Flutter project.」，建议后续补成正式描述。

---

## 3. 整体架构

### 3.1 分层模型

```
┌───────────────────────── Flutter App ─────────────────────────┐
│  UI (features/*)                                               │
│      ▲ 仅消费状态、转发用户操作                                  │
│  Riverpod (providers/*)  —— 状态层，连接 UI 与数据              │
│      ▲                                                         │
│  Repository (data/*)  —— 业务/数据聚合                          │
│      ├── LocalFileService   (本机文件系统抽象，封装 dart:io)     │
│      ├── RemoteFileClient   (Dio 封装，访问对方 HttpServer)     │
│      └── DiscoveryService   (mDNS 广播+扫描)                    │
│      ▲                               ▲                          │
│  MediaHttpServer (server/*)          │ (HTTP over LAN)          │
│  本机对外发布，读取本机文件系统  ◄────┘ 对方客户端请求            │
└────────────────────────────────────────────────────────────────┘
```

数据流向：

- **本机浏览**：`UI → Provider → LocalFileService → dart:io 文件系统`
- **浏览远程设备**：`UI → Provider → RemoteFileClient(Dio) → 对方 MediaHttpServer`
- **被别人浏览**：`对方请求 → 本机 MediaHttpServer → 读本机文件系统 → 返回 JSON/文件流`
- **互相发现**：`DiscoveryService` 双向广播+扫描 → 拿到 `ip:port` → 交给 `RemoteFileClient`

### 3.2 关键设计原则

1. **上层只依赖抽象层**：UI 与 Provider 不直接调用 `dart:io`，统一走 `LocalFileService`/`RemoteFileClient`，未来切 SAF/MediaStore 只改数据层。
2. **播放内核单点封装**：整个项目只有 `lib/features/media_player/services/media_player_service.dart` 直接依赖 `media_kit`；上层拿到的是本项目自己的 `PlayerState`/`MediaItem` 类型。换内核只需重写这一个文件。
3. **状态来源单一**：播放状态全部订阅内核状态流，**禁止用 Timer 轮询**维护 `isPlaying/position`，避免 UI 与内核错位。
4. **响应式而非设备类型判断**：平板/横屏用**最短边**（`MediaQuery.sizeOf(context).shortestSide >= 600`）判定，双栏需「平板 + 横屏 + 宽≥840」三条件同时满足；横屏、双栏、全屏是三个独立状态，互不替代（`lib/core/responsive.dart`）。
5. **设计 Token 约束**：所有样式走 `lib/core/tokens.dart`（AppPalette/AppSpacing/AppRadius/AppMotion/AppShadow），禁止魔法数字、魔法色值、手写 `BoxShadow`。
6. **统一错误封装**：`Result<T>` 密封类（Ok/Err），避免散落的 `try/catch` 与空值判断。

### 3.3 目录结构（实际）

```
lib/
├── main.dart                      入口：MediaKit 初始化、前台服务端口、通知渠道
├── app/
│   ├── app.dart                   MaterialApp + 主题 + 路由装配
│   └── router.dart                go_router 路由表
├── core/                         基础设施与设计 Token
│   ├── constants.dart            端口/basePort=8848、mDNS 类型、版本、持久化键
│   ├── file_type.dart            EntryType + 后缀/MIME 映射
│   ├── file_view_mode.dart       列表/网格视图枚举
│   ├── seek_sensitivity.dart     拖动灵敏度枚举（120/200/320）
│   ├── video_play_mode.dart      顺序/随机/循环连播枚举
│   ├── video_resume.dart         断点续播持久化
│   ├── video_thumbnail.dart      视频缩略图提取
│   ├── media_metadata.dart       媒体元数据
│   ├── responsive.dart           平板/横屏/双栏判定（最短边）
│   ├── breakpoints.dart          宽度断点常量
│   ├── tokens.dart               设计 Token（色彩/间距/圆角/动效/阴影）
│   ├── result.dart               Result<T> 密封类
│   ├── format.dart               格式化工具
│   ├── path_utils.dart           相对路径规范化（防穿越）
│   └── prompts.dart              文案常量
├── data/
│   ├── models/{file_entry,device_info}.dart
│   ├── local/local_file_service.dart     列/建/改名/删/搜
│   └── remote/{remote_file_client,discovery_service}.dart
├── server/
│   ├── media_http_server.dart    HttpServer 启停 + 端口重试
│   ├── router.dart               路径前缀分发
│   ├── server_context.dart       共享上下文 + PathGuard 防穿越 + 响应工具
│   ├── foreground_service.dart   前台服务保活
│   └── handlers/{info,list,file,search,thumbnail}_handler.dart
├── providers/                    Riverpod 状态层
│   ├── services_provider.dart    服务单例
│   ├── server_provider.dart      服务器启停 + 注册/前台
│   ├── discovery_provider.dart   mDNS 流 + 手动设备 + 合并去重
│   ├── browser_provider.dart     本地/远程浏览 + 选中项 + 路径
│   └── settings_provider.dart    设置（持久化）
├── features/
│   ├── home/home_shell.dart      导航壳（底部/侧边栏切换）
│   ├── local_browser/            本地浏览页
│   ├── remote_browser/           远程浏览页
│   ├── devices/                  设备发现页
│   ├── media_viewer/             图片查看（image_view/image_viewer_page/media_source）
│   ├── media_player/             视频播放模块（分层，见 3.4）
│   ├── settings/                 设置页
│   └── shared/                   复用组件（网格/列表/缩略图/双栏壳/状态视图）
└── permissions/
    └── storage_permission.dart   存储权限封装
```

### 3.4 视频播放模块分层（最复杂、最有价值的部分）

```
MediaItem / PlayerState（models）
        ▼
MediaPlayerService（唯一直接依赖 media_kit 的一层）
        ▼
MediaPlayerController（状态流 + 连播 + 断点 + 生命周期）
        ▼
VideoPlayerPage / VideoPreviewPanel（页面与内嵌预览）
        ▼
MediaPlayerView + 控件层 / 手势层 / 指示器（widgets）
```

- **状态机**：`idle / loading / playing / paused / buffering / completed / error`。
- **手势规格**：单击显隐控件；双击左 35% 后退 10s / 中央 30% 播放暂停 / 右 35% 前进 10s；长按临时倍速；左右拖动 seek（灵敏度可在设置页调）；左半屏上下调亮度、右半屏调音量。
- **可扩展位**：字幕 / 音轨 / 投放等可挂在 `MediaPlayerService` 之上，页面层无需改动。

---

## 4. 配置说明（Android）

### 4.1 `pubspec.yaml`

- `version: 2.1.1+4`，`environment.sdk: ^3.13.2`。
- `description` 仍为模板默认，建议更正。

### 4.2 `android/app/build.gradle.kts`

```kotlin
namespace = "com.faner.app.faner"
compileSdk = 37
minSdk = 26          // Android 8.0
targetSdk = flutter.targetSdkVersion
release {
    isMinifyEnabled = true        // 含 R8 混淆
    isShrinkResources = false
    proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"),
                  "proguard-rules.pro")
}
```

### 4.3 `android/app/proguard-rules.pro`（已修复 release 闪退）

release 下 R8 会删掉 WorkManager 的 Room 实现类，导致启动崩溃
`Failed to create an instance of androidx.work.impl.WorkDatabase`（Google issue 348590028）。
已通过 `-keep` 保留 `androidx.work.**` / `androidx.room.**` / `androidx.startup.**` /
`com.pravera.flutter_foreground_task.**` / `com.alexmercerind.media_kit_video.**` 解决。

> 构建命令：轻包体用 `flutter build apk --release --target-platform android-arm64 --split-per-abi`（libmpv 使包体显著增大，务必 split-per-abi）。

### 4.4 `AndroidManifest.xml` 关键项

- **明文流量**：`<application android:usesCleartextTraffic="true">`（局域网 HTTP 必需，否则请求被系统静默拦截）。
- `requestLegacyExternalStorage="true"`。
- 权限：`INTERNET`、`ACCESS_WIFI_STATE`、`ACCESS_NETWORK_STATE`、`CHANGE_WIFI_MULTICAST_STATE`、`MANAGE_EXTERNAL_STORAGE`、`READ_MEDIA_IMAGES/VIDEO/AUDIO`、`FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_DATA_SYNC`、`WAKE_LOCK`、`POST_NOTIFICATIONS`。
- 前台服务声明：`com.pravera.flutter_foreground_task.service.ForegroundService`，`foregroundServiceType="dataSync"`。

### 4.5 `analysis_options.yaml`

- 继承 `flutter_lints`，排除 `build/`、`android/`、`markdown/`。

---

## 5. 网络协议设计（HTTP API）

固定端口 `8848`（被占用自动 +1 重试，最多 5 次）；mDNS 服务类型 `_lanmedia._tcp`。

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/info` | GET | 返回 `deviceName` / `appVersion` / `sharedRootLabel` |
| `/api/list?path=` | GET | 返回当前目录条目；路径不存在 `404`；含 `..` 穿越 `403` |
| `/api/file?path=` | GET | **核心**：支持 `Range` 流式读取，全程 `openRead` 不落内存 |
| `/api/search?q=` | GET | 递归搜索（限深 8、限 200 结果） |
| `/api/thumbnail?path=` | GET | 视频缩略图 |

**Range 实现要点**（`handlers/file_handler.dart`）：

1. 解析 `Range: bytes=start-end`；
2. 无 Range → `200` 整文件；有 Range → 校验后 `206 Partial Content`，带 `Content-Range` / `Accept-Ranges`；
3. 后缀范围 `bytes=-N`、非法范围 `416`；
4. 全部 `file.openRead(start, end+1).addStream(...)`，禁止 `readAsBytes()`。

**Content-Type**：按后缀映射到 `image/*`、`video/*`、`audio/*`、`application/octet-stream`。

**路径穿越防护**（安全底线，两道防线）：

- `core/path_utils.dart` 的 `normalizeRelPath`：相对路径一旦含 `..` 段即返回 `null`；
- `server/server_context.dart` 的 `PathGuard`：把相对路径解析到 `root` 内绝对路径，越界返回 `null`，`list`/`file`/`search`/`thumbnail` 各 handler 均先过此关，非法返回 `403`。

---

## 6. 状态管理与关键 Provider

| Provider | 类型 | 职责 |
|---|---|---|
| `localFileServiceProvider` / `remoteFileClientProvider` / `discoveryServiceProvider` | `Provider` | 服务单例 |
| `serverControllerProvider` | `NotifierProvider` | 控制 `MediaHttpServer` 启停，启动后联动前台服务 + mDNS 注册 |
| `discoveryProvider` | `StreamProvider` | mDNS 扫描到的设备列表流 |
| `manualDevicesProvider` | `NotifierProvider` | 手动设备（持久化到 `shared_preferences`） |
| `deviceListProvider` | `Provider` | 合并 mDNS + 手动，去重排序 |
| `localPathProvider` / `remotePathProvider` | `NotifierProvider` | 当前浏览路径（全局，供路由 `onExit` 拦截系统返回键逐层回退目录） |
| `localBrowserProvider` / `remoteBrowserProvider` | `FutureProvider.autoDispose.family` | 按路径/设备加载文件列表 |
| `selectedEntryProvider` | `NotifierProvider` | 平板双栏「当前选中项」，左右面板共享 |
| `settingsProvider` | `NotifierProvider` | 设备名、共享开关、主题、视图、连播、灵敏度、断点续播等 |

**启动链路**（`home_shell.dart`）：加载设置 → 若 `serverEnabled` 则 `serverControllerProvider.start()` → 启动 HttpServer + 前台服务 + mDNS 注册。

---

## 7. 开发规划与里程碑

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | 本地文件浏览器（权限、缩略图、分类、搜索、增删改查） | ✅ |
| M2 | 最小 Server + 手动互联 | ✅ |
| M3 | Range 支持 + 视频播放打通 | ✅ |
| M4 | mDNS 自动发现 | ✅ |
| M5 | 平板响应式布局 | ✅ |
| M6 | 后台保活 | ✅ |
| M7 | 体验打磨（图片缓存/缩略图缓存等，部分完成） | 🔶 |

> 现状：M1–M6 全部完成；M7 中图片缓存、断点续播、连播模式、灵敏度调节已落地，缩略图生成缓存、收藏夹、最近连接历史等仍可选做。

---

## 8. 验收要点（回归测试清单）

- **安全底线**：`/api/list?path=../../../etc`、`/api/file?path=..%2f..%2f..` 必须 `403`。
- **Range 正确性**：`curl -r 1000-2000`，验证 `206` + `Content-Range`。
- **明文流量**：确认 `usesCleartextTraffic="true"`，双机互通不被系统拦截。
- **大文件性能**：≥1GB 视频拖动进度条无需重缓冲全片，跳转 1–2s 内。
- **弱网/断连**：关 Wi-Fi 或对方退出后，客户端有超时与友好提示，不一直转圈/崩溃。
- **端口冲突**：模拟 8848 占用，验证自动 `+1` 重试并重新广播。
- **平板专项**：横竖屏切换、分屏小窗、不同断点网格列数变化。
- **前台保活**：锁屏 5 分钟后对方仍能拉取/播放。
- **release 构建**：`flutter build apk --release --split-per-abi` 启动无 `WorkDatabase` 闪退。

---

## 9. 已知风险与应对

| 风险 | 应对 |
|---|---|
| HTTP 明文被系统拦截 | `usesCleartextTraffic=true` |
| mDNS 在 AP 隔离/访客网下失效 | 保留手动 IP 连接兜底 |
| 厂商 ROM 后台杀进程激进 | 前台服务 + WiFi 锁 + 引导关闭电池优化 |
| Android 分区存储限制 | `MANAGE_EXTERNAL_STORAGE` + 文件系统抽象层 |
| Android 13+ 细分媒体权限 | 一并检查 `READ_MEDIA_IMAGES/VIDEO/AUDIO` |
| 视频过大内存爆掉 | file_handler 流式读取，禁止 `readAsBytes()` |
| 局域网多 App 互相干扰 | 自定义 mDNS 类型 `_lanmedia._tcp` |
| 平板分屏/多窗口状态丢失 | 状态放 Riverpod 而非 Widget 局部 State |
| release R8 删 WorkManager Room 类 | proguard 保留 `androidx.work/room/startup` |
| 部分 ROM 组播受限 | 优先排查此项而非业务代码 |

---

## 10. 后续可优化项（建议）

1. 补全 M7 剩余项：缩略图生成缓存、收藏夹、最近连接历史 UI。
2. 修正 `pubspec.yaml` 的 `description`。
3. `pubspec.lock` 已锁定，建议定期 `flutter pub outdated` 跟新。
4. 补充单元测试/Widget 测试覆盖（目前 `test/` 仅 2 个文件，可增强 Server handlers、PathGuard、Provider 的测试）。
5. 考虑为 `localPathProvider`/`remotePathProvider` 的返回键拦截补充注释化测试（go_router + PopScope 兼容坑已用 `onExit` 规避）。
6. 视频模块可进一步扩展字幕/音轨/投屏（已在 `MediaPlayerService` 预留扩展位）。
